## 2024-05-01 - Add aria-labels to data-tip buttons\n**Learning:** The codebase frequently uses `data-tip` attributes for tooltips on icon-only buttons, but these buttons lack `aria-label` attributes for screen readers.\n**Action:** Found 20 missing aria-labels on buttons with `data-tip` in TeacherControls.tsx. I will add aria-labels to all buttons that use data-tip in TeacherControls to improve accessibility.
