node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.rooms/

# Server env file for the Lightsail deploy — secrets, never commit.
deploy/mathslive.env

# Playwright output. Traces and screenshots are kept only for a failure,
# and only long enough to look at.
test-results/
playwright-report/
.typecheck-ok
dist-server/
